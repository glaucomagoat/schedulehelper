[build]
  publish = "."
  functions = "netlify/functions"
  edge_functions = "netlify/edge-functions"
  # No secrets are embedded in index.html. All auth uses per-user session JWTs
  # signed server-side with SESSION_SECRET. Set SESSION_SECRET, DEV_USERNAME,
  # DEV_PASSWORD, CVE_USER, CVE_PASS, and ANTHROPIC_API_KEY in Netlify dashboard.
  command = "npm install"

[[headers]]
  for = "/*"
  [headers.values]
    Strict-Transport-Security = "max-age=31536000; includeSubDomains"
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

[functions]
  node_bundler = "esbuild"

[functions."storage-proxy"]
  timeout = 10

# Sending to the whole roster across two channels takes longer than the default.
# The cron itself stays fast because it only decides and delegates.
[functions."tech-notify"]
  timeout = 26

# Short link for the technician day view. The signed token already rides in ?t=,
# so keeping the path short keeps the whole URL comfortably inside one SMS segment
# if SMS is ever added as a channel.
[[redirects]]
  from = "/d"
  to = "/.netlify/functions/tech-day"
  status = 200

# Route /.netlify/functions/claude-proxy to the Edge Function instead of
# the serverless function. Edge Functions support true response streaming,
# which is required for AI schedule generation to work without timeouts.
# The old netlify/functions/claude-proxy.mjs is kept but no longer used.
[[edge_functions]]
  path = "/.netlify/functions/claude-proxy"
  function = "claude-proxy"
